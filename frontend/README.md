## Frontend Setup

### Install & Run
```bash
cd frontend
npm install
npm run dev
```

### Environment Variables
- Copy `.env.example` to `.env`.
- Set `VITE_API_BASE` to the FastAPI origin if it differs from `http://localhost:8000/api`.
